{
  "compilerOptions": {
    "target": "es2016",                  /* Especifica la versión de JavaScript emitido. */
    "module": "commonjs",                /* Especifica el tipo de módulo generado. */
    "jsx": "react-jsx",                  /* Habilita el soporte para JSX en React. */
    "esModuleInterop": true,             /* Permite la interoperabilidad con módulos CommonJS. */
    "forceConsistentCasingInFileNames": true, /* Asegura consistencia en el uso de mayúsculas/minúsculas en los nombres de archivos. */
    "strict": true,                      /* Habilita todas las comprobaciones estrictas de TypeScript. */
    "skipLibCheck": true                 /* Omite la verificación de los archivos de declaración `.d.ts`. */
  }
}
